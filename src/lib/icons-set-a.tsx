/**
 * Icons A–Ch (v18.0.0)
 * Bundler-safe: dangerouslySetInnerHTML bypasses jsxs transform for SVG children
 */
import type { IconProps } from './icon-base';

export function Activity({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function ArrowLeft({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="m12 19-7-7 7-7" /><path d="M19 12H5" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function ArrowRight({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M5 12h14" /><path d="m12 5 7 7-7 7" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function ArrowUp({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="m5 12 7-7 7 7" /><path d="M12 19V5" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Book({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function BookOpen({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z" /><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Bookmark({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="m19 21-7-4-7 4V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2v16z" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Brain({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M12 5a3 3 0 1 0-5.997.125 4 4 0 0 0-2.526 5.77 4 4 0 0 0 .556 6.588A4 4 0 1 0 12 18Z" /><path d="M12 5a3 3 0 1 1 5.997.125 4 4 0 0 1 2.526 5.77 4 4 0 0 1-.556 6.588A4 4 0 1 1 12 18Z" /><path d="M15 13a4.5 4.5 0 0 1-3-4 4.5 4.5 0 0 1-3 4" /><path d="M17.599 6.5a3 3 0 0 0 .399-1.375" /><path d="M6.003 5.125A3 3 0 0 0 6.401 6.5" /><path d="M3.477 10.896a4 4 0 0 1 .585-.396" /><path d="M19.938 10.5a4 4 0 0 1 .585.396" /><path d="M6 18a4 4 0 0 1-1.967-.516" /><path d="M19.967 17.484A4 4 0 0 1 18 18" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function BusFront({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M4 6 2 7" /><path d="M10 6h4" /><path d="m22 7-2-1" /><rect width="16" height="16" x="4" y="3" rx="2" /><path d="M4 11h16" /><path d="M8 15h.01" /><path d="M16 15h.01" /><path d="M6 19v2" /><path d="M18 19v2" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Calendar({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M8 2v4" /><path d="M16 2v4" /><rect width="18" height="18" x="3" y="4" rx="2" /><path d="M3 10h18" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Camera({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M14.5 4h-5L7 7H4a2 2 0 0 0-2 2v9a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2h-3l-2.5-3z" /><circle cx="12" cy="13" r="3" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function CarFront({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="m21 8-2 2-1.5-3.7A2 2 0 0 0 15.646 5H8.4a2 2 0 0 0-1.903 1.257L5 10 3 8" /><path d="M7 14h.01" /><path d="M17 14h.01" /><rect width="18" height="8" x="3" y="10" rx="2" /><path d="M5 18v2" /><path d="M19 18v2" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Check({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M20 6 9 17l-5-5" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function ChevronDown({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="m6 9 6 6 6-6" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function ChevronLeft({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="m15 18-6-6 6-6" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function ChevronRight({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="m9 18 6-6-6-6" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function ChevronUp({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="m18 15-6-6-6 6" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Circle({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<circle cx="12" cy="12" r="10" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}
