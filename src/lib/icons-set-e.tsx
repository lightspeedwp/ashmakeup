/**
 * Icons Sun–ZoomOut (v15.0.0)
 * Bundler-safe: dangerouslySetInnerHTML bypasses jsxs transform for SVG children
 */
import type { IconProps } from './icon-base';

export function Sun({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<circle cx="12" cy="12" r="4" /><path d="M12 2v2" /><path d="M12 20v2" /><path d="m4.93 4.93 1.41 1.41" /><path d="m17.66 17.66 1.41 1.41" /><path d="M2 12h2" /><path d="M20 12h2" /><path d="m6.34 17.66-1.41 1.41" /><path d="m19.07 4.93-1.41 1.41" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Tag({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z" /><circle cx="7.5" cy="7.5" r=".5" fill="currentColor" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function ThumbsUp({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M7 10v12" /><path d="M15 5.88 14 10h5.83a2 2 0 0 1 1.92 2.56l-2.33 8A2 2 0 0 1 17.5 22H4a2 2 0 0 1-2-2v-8a2 2 0 0 1 2-2h2.76a2 2 0 0 0 1.79-1.11L12 2h0a3.13 3.13 0 0 1 3 3.88Z" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function TrainFront({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M8 3.1V7a4 4 0 0 0 8 0V3.1" /><path d="m9 15-1-1" /><path d="m15 15 1-1" /><path d="M9 19c-2.8 0-5-2.2-5-5v-4a8 8 0 0 1 16 0v4c0 2.8-2.2 5-5 5Z" /><path d="m8 19-2 3" /><path d="m16 19 2 3" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Trash2({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M3 6h18" /><path d="M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6" /><path d="M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2" /><line x1="10" x2="10" y1="11" y2="17" /><line x1="14" x2="14" y1="11" y2="17" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function TriangleAlert({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3Z" /><path d="M12 9v4" /><path d="M12 17h.01" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Type({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<polyline points="4 7 4 4 20 4 20 7" /><line x1="9" x2="15" y1="20" y2="20" /><line x1="12" x2="12" y1="4" y2="20" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function User({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2" /><circle cx="12" cy="7" r="4" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Volume2({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5" /><path d="M15.54 8.46a5 5 0 0 1 0 7.07" /><path d="M19.07 4.93a10 10 0 0 1 0 14.14" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function VolumeX({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5" /><line x1="22" x2="16" y1="9" y2="15" /><line x1="16" x2="22" y1="9" y2="15" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Wifi({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M12 20h.01" /><path d="M2 8.82a15 15 0 0 1 20 0" /><path d="M5 12.859a10 10 0 0 1 14 0" /><path d="M8.5 16.429a5 5 0 0 1 7 0" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function WifiOff({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M12 20h.01" /><path d="M8.5 16.429a5 5 0 0 1 7 0" /><path d="M5 12.859a10 10 0 0 1 5.17-2.69" /><path d="M13.41 10.25a10 10 0 0 1 5.59 2.61" /><path d="M2 8.82a15 15 0 0 1 4.17-2.65" /><path d="M10.66 5c4.01-.36 8.14.9 11.34 3.76" /><line x1="2" x2="22" y1="2" y2="22" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function X({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M18 6 6 18" /><path d="m6 6 12 12" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Zap({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M4 14a1 1 0 0 1-.78-1.63l9.9-10.2a.5.5 0 0 1 .86.46l-1.92 6.02A1 1 0 0 0 13 10h7a1 1 0 0 1 .78 1.63l-9.9 10.2a.5.5 0 0 1-.86-.46l1.92-6.02A1 1 0 0 0 11 14z" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function ZoomIn({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<circle cx="11" cy="11" r="8" /><line x1="21" x2="16.65" y1="21" y2="16.65" /><line x1="11" x2="11" y1="8" y2="14" /><line x1="8" x2="14" y1="11" y2="11" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function ZoomOut({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<circle cx="11" cy="11" r="8" /><line x1="21" x2="16.65" y1="21" y2="16.65" /><line x1="8" x2="14" y1="11" y2="11" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}
