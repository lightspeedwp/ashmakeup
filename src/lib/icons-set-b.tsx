/**
 * Icons CircleCheck–Heart (v15.0.0)
 * Bundler-safe: dangerouslySetInnerHTML bypasses jsxs transform for SVG children
 */
import type { IconProps } from './icon-base';

export function CircleCheck({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<circle cx="12" cy="12" r="10" /><path d="m9 12 2 2 4-4" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function CircleHelp({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<circle cx="12" cy="12" r="10" /><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3" /><path d="M12 17h.01" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function CirclePlay({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<circle cx="12" cy="12" r="10" /><polygon points="10 8 16 12 10 16" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function CircleX({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<circle cx="12" cy="12" r="10" /><path d="m15 9-6 6" /><path d="m9 9 6 6" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Clock({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<circle cx="12" cy="12" r="10" /><polyline points="12 6 12 12 16 14" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Cloud({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M17.5 19H9a7 7 0 1 1 6.71-9h1.79a4.5 4.5 0 1 1 0 9Z" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Code({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<polyline points="16 18 22 12 16 6" /><polyline points="8 6 2 12 8 18" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Copy({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<rect width="14" height="14" x="8" y="8" rx="2" ry="2" /><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Download({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" /><polyline points="7 10 12 15 17 10" /><line x1="12" x2="12" y1="15" y2="3" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Ellipsis({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<circle cx="12" cy="12" r="1" /><circle cx="19" cy="12" r="1" /><circle cx="5" cy="12" r="1" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function ExternalLink({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M15 3h6v6" /><path d="M10 14 21 3" /><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Eye({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7Z" /><circle cx="12" cy="12" r="3" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function FileCode({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z" /><path d="M14 2v4a2 2 0 0 0 2 2h4" /><path d="m10 13-2 2 2 2" /><path d="m14 17 2-2-2-2" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function FileText({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z" /><path d="M14 2v4a2 2 0 0 0 2 2h4" /><path d="M10 9H8" /><path d="M16 13H8" /><path d="M16 17H8" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function FolderOpen({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="m6 14 1.5-2.9A2 2 0 0 1 9.24 10H20a2 2 0 0 1 1.94 2.5l-1.54 6a2 2 0 0 1-1.95 1.5H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h3.9a2 2 0 0 1 1.69.9l.81 1.2a2 2 0 0 0 1.67.9H18a2 2 0 0 1 2 2v2" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function GraduationCap({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M21.42 10.922a1 1 0 0 0-.019-1.838L12.83 5.18a2 2 0 0 0-1.66 0L2.6 9.08a1 1 0 0 0 0 1.832l8.57 3.908a2 2 0 0 0 1.66 0z" /><path d="M22 10v6" /><path d="M6 12.5V16a6 3 0 0 0 12 0v-3.5" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Headphones({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M3 14h3a2 2 0 0 1 2 2v3a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-7a9 9 0 0 1 18 0v7a2 2 0 0 1-2 2h-1a2 2 0 0 1-2-2v-3a2 2 0 0 1 2-2h3" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}

export function Heart({ color, strokeWidth: swp, size: sz, width: wp, height: hp, className: cn, style: st, onClick: oc }: IconProps) {
  const sc = color ? color : "currentColor";
  const sw = swp ? swp : 2;
  const base = sz ? sz : 24;
  const ww = wp ? wp : base;
  const hh = hp ? hp : base;
  const inner = '<path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z" />';
  const markup = { __html: inner };
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke={sc} strokeWidth={sw} strokeLinecap="round" strokeLinejoin="round" width={ww} height={hh} className={cn} style={st} onClick={oc} dangerouslySetInnerHTML={markup} />
  );
}
