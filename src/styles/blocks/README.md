# Block Styles

This directory contains dedicated stylesheets for individual components/blocks, aligned with the WordPress block theme architecture.

## Structure
- Each file should correspond to a specific component or block.
- Use BEM naming conventions.
- Do not use Tailwind utility classes.
- Use global CSS variables from `/styles/globals.css`.
