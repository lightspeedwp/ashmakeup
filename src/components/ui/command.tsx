/**
 * @fileoverview Command stub (unused – original cmdk import removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

export function Command({ children }: { children?: React.ReactNode }) {
  return <div>{children}</div>;
}