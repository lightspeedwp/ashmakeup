/**
 * @fileoverview Drawer stub (unused – original vaul import removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

export function Drawer({ children }: { children?: React.ReactNode }) {
  return <>{children}</>;
}
