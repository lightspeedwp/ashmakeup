/**
 * @fileoverview Resizable stub (unused – original react-resizable-panels import removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

export function ResizablePanel({ children }: { children?: React.ReactNode }) {
  return <div>{children}</div>;
}