/**
 * @fileoverview Badge stub (unused – original badge component import removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

function Badge({ children }: { children?: React.ReactNode }) { return <span>{children}</span>; }

export { Badge };