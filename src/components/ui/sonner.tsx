/**
 * @fileoverview Toaster stub (unused – original next-themes import removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

export function Toaster() {
  return null;
}
