/**
 * @fileoverview Form stub (unused – original react-hook-form import removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

export function Form({ children }: { children?: React.ReactNode }) {
  return <>{children}</>;
}
