/**
 * @fileoverview Calendar stub (unused – original react-day-picker import removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

export function Calendar() {
  return null;
}
