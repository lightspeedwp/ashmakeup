/**
 * @fileoverview Sheet stub (unused – @radix-ui/react-dialog import removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

const noop = ({ children }: { children?: React.ReactNode }) => <>{children}</>;

const Sheet = noop;
const SheetTrigger = noop;
const SheetClose = noop;
const SheetContent = noop;
const SheetHeader = noop;
const SheetFooter = noop;
const SheetTitle = noop;
const SheetDescription = noop;

export {
  Sheet,
  SheetTrigger,
  SheetClose,
  SheetContent,
  SheetHeader,
  SheetFooter,
  SheetTitle,
  SheetDescription,
};
