/**
 * @fileoverview Sidebar stub (unused – @radix-ui/react-slot and internal shadcn imports removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

const noop = ({ children }: { children?: React.ReactNode }) => <>{children}</>;

const Sidebar = noop;
const SidebarContent = noop;
const SidebarFooter = noop;
const SidebarGroup = noop;
const SidebarGroupAction = noop;
const SidebarGroupContent = noop;
const SidebarGroupLabel = noop;
const SidebarHeader = noop;
const SidebarInput = () => null;
const SidebarInset = noop;
const SidebarMenu = noop;
const SidebarMenuAction = noop;
const SidebarMenuBadge = noop;
const SidebarMenuButton = noop;
const SidebarMenuItem = noop;
const SidebarMenuSkeleton = () => null;
const SidebarMenuSub = noop;
const SidebarMenuSubButton = noop;
const SidebarMenuSubItem = noop;
const SidebarProvider = noop;
const SidebarRail = () => null;
const SidebarSeparator = () => null;
const SidebarTrigger = noop;
function useSidebar() { return { open: false, setOpen: () => {}, toggleSidebar: () => {} }; }

export {
  Sidebar,
  SidebarContent,
  SidebarFooter,
  SidebarGroup,
  SidebarGroupAction,
  SidebarGroupContent,
  SidebarGroupLabel,
  SidebarHeader,
  SidebarInput,
  SidebarInset,
  SidebarMenu,
  SidebarMenuAction,
  SidebarMenuBadge,
  SidebarMenuButton,
  SidebarMenuItem,
  SidebarMenuSkeleton,
  SidebarMenuSub,
  SidebarMenuSubButton,
  SidebarMenuSubItem,
  SidebarProvider,
  SidebarRail,
  SidebarSeparator,
  SidebarTrigger,
  useSidebar,
};
