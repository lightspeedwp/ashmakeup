/**
 * @fileoverview Sidebar stub (unused – @radix-ui/react-slot import removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

function noop({ children }: { children?: React.ReactNode }) { return <span>{children}</span>; }

var Sidebar = noop;
var SidebarContent = noop;
var SidebarFooter = noop;
var SidebarGroup = noop;
var SidebarGroupContent = noop;
var SidebarGroupLabel = noop;
var SidebarHeader = noop;
var SidebarMenu = noop;
var SidebarMenuButton = noop;
var SidebarMenuItem = noop;
var SidebarProvider = noop;
var SidebarTrigger = noop;

export {
  Sidebar,
  SidebarContent,
  SidebarFooter,
  SidebarGroup,
  SidebarGroupContent,
  SidebarGroupLabel,
  SidebarHeader,
  SidebarMenu,
  SidebarMenuButton,
  SidebarMenuItem,
  SidebarProvider,
  SidebarTrigger,
};