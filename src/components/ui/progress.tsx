/**
 * @fileoverview Progress stub (unused – @radix-ui/react-progress import removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

function Progress(props: React.ComponentProps<"div">) {
  return <div {...props} />;
}

export { Progress };
