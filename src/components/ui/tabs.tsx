/**
 * @fileoverview Tabs stub (unused – @radix-ui/react-tabs import removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

function noop({ children }: { children?: React.ReactNode }) { return <span>{children}</span>; }

var Tabs = noop;
var TabsList = noop;
var TabsTrigger = noop;
var TabsContent = noop;

export { Tabs, TabsList, TabsTrigger, TabsContent };