/**
 * @fileoverview HoverCard stub (unused – @radix-ui/react-hover-card import removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

function noop({ children }: { children?: React.ReactNode }) { return <span>{children}</span>; }

var HoverCard = noop;
var HoverCardTrigger = noop;
var HoverCardContent = noop;

export { HoverCard, HoverCardTrigger, HoverCardContent };