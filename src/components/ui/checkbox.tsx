/**
 * @fileoverview Checkbox stub (unused – @radix-ui/react-checkbox import removed
 *               to prevent async_hooks runtime errors via esm.sh)
 */

import * as React from "react";

function Checkbox(props: React.ComponentProps<"input">) {
  return <input type="checkbox" {...props} />;
}

export { Checkbox };
